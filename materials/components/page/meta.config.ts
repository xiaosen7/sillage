import { defineConfig } from "@sillage/meta";

export default defineConfig({
  name: "Page",
  title: "页面",
  box: {
    width: {
      mode: "auto",
    },
    height: { mode: "auto" },
  },
  internal: true,
});
