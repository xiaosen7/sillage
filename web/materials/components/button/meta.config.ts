import { defineConfig } from "@sillage/meta";

export default defineConfig({
  name: "button",
  title: "按钮",
  box: {
    width: {
      mode: "auto",
    },
    height: {
      mode: "auto",
    },
  },
});
