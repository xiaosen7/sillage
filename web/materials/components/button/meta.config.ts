import defineConfig from "@sillage/define-component-meta";

export default defineConfig({
  name: "button",
  title: "按钮",
  box: {
    width: {
      mode: "auto",
    },
    height: {
      mode: "auto",
    },
  },
});
