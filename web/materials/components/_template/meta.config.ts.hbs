import defineConfig from "@sillage/define-component-meta"; export default
defineConfig({ name: "{{pascalCase name}}", title: "{{title}}", box: { width: {
mode: "auto", }, height: { mode: "auto", }, }, internal:
{{internal}}
});