import { CodeEditor } from "../components/CodeEditor";

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface CodelessProps {}

export default function Codeless(props: CodelessProps) {
  return <CodeEditor />;
}
