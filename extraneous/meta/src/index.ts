import type { ComponentMetaConfig } from "./types";

function defineConfig(metaConfig: ComponentMetaConfig): ComponentMetaConfig {
  return metaConfig;
}

export { defineConfig };

export { type ComponentMetaConfig } from "./types";
